역할별 탭 요청 헤더와 세션 격리, 운영자 로그인 시 데모 세션 발급, 기본 로그인 보존, 사진 역할 선택과 테스트를 수행한다. 빌드 플래그와 서버 opt-in 응답 모두 확인한다.

완료: API 역할 헤더 및 역할 변경 시 CSRF 재발급, 역할 경계를 넘을 때 전체 페이지 이동, 서버 opt-in일 때 3개 역할 새 탭 링크, 운영자 로그인 bootstrap, 공개 첫401에서만 기본 클라이언트 bootstrap, 서버 모드 공개 배너, 사진 역할 query 전달. HQ/지역 관리자는 탭별 기본 세션 선택을 유지한다.
공개 시연 사진 버튼은 공개 제출 화면에서만 노출하고 기존 파일 검증·축소·미리보기 경로를 사용한다. 사진 명칭은 AI 생성 시연 사진이다.
검증: API client 3개, 프론트 33개 통과. hosted/multi-role/public 빌드 플래그 모두 true로 TypeScript 및 Vite 빌드 통과. 실제 배포·실제 인증·AI 호출은 root 담당이며 여기서는 실행하지 않았다.
