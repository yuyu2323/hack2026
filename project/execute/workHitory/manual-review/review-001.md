# MANUAL-REVIEW-001 독립 내용/정적검토

- 상태 RUNNING. 현재는 소스 기반 초안 검토이며 최종 매뉴얼 인수 완료가 아니다.
- 본문/이미지 작성자는 product_design, 검수자는 contract_ai. 본문 파일을 수정하지 않고 작성자에게 조치 요청한다.
- 4개 HTML의 UTF8/lang=ko, 목차/ID 중복·로컬 링크·외부의존성을 독립 확인했다. 결과 static-001.json. 화면 이미지0개이며 문서가 이를 NOT_RUN으로 명시하므로 허위 완료 주장은 없다.
- 역할5, 각기 다른 실제 동선(01 단일폼·탭·관리행,03 사진/근거·Reference갤러리,04 하단탐색·4단계,05 범위탐색·비교), 파일/질문 제약, unknown/기술실패·null, 버전/재제출, 운영 권한 경계, 실AI/Mock, 로컬계정ID와 비밀번호 보관위치, 전체 시연 순서가 포함되어 있다.

## 발견/요청

| ID | 관찰 | 조치·상태 |
|---|---|---|
| MR-001 | 01 계정 생성 설명이 제출명칭 계정 등록만 제시해 앞선 일반 계정 등록 펼침 단계 누락 | 작성자에 실제 펼침→입력→제출 순서 요청 |
| MR-002 | 동적 버튼명 뒤 조사 오류: 사진으로 점검하기을, 계정 등록로, 연결 저장로 등 | 4개 공통 문장을 버튼을/버튼으로 형태로 교정 요청 |
| MR-003 | 공통 사진 오류 안내의 사진 다시 보기 버튼은01 Photos에 없음 | 실제 있는 상태조회/목록에서 다시 열기로 시안별 안내 수정 요청 |

## 남은 검증

최종 시안별 vp 캡처와 직접 UI 대조, Browser에서 목차 이동/전체폭/모바일/오프라인 이미지·글자 배치를 확인한다. HTML 정적검사는 이를 대신하지 않는다. 실제 지연/기능 인수는 root의 시안별 결과가 인계된 뒤 각 설명·링크가 정확한지 확인한다.

## 수정 재검수

작성자의 MAN-D01 수정 이후 원문을 다시 확인했다. MR-001 일반 계정 등록 펼침→입력→계정 등록 제출 순서, MR-002 버튼 조사 교정, MR-003 사진 오류 시 목록 재열기/실제 상태 확인 동작 안내가 반영되었다. 세 내용 지적은 해소되었다. 최종 캡처와 문서 Browser 검수는 계속 대기한다.

## 시안 01 실제 이미지 반영 재검수

8개 매뉴얼 이미지의 바이트·SHA-256이 root 원본과 일치하고 HTML의 alt·캡션이 manifest와 일치한다. 각 원본은 앞선 독립 디자인 검토에서 직접 보았고, 현재 캡션은 보이는 영역만 설명한다. 문제가 있는 재제출 미리보기 캡처는 포함하지 않았다. 로컬 링크 43개에 깨진 경로/목차는 없다. 결과는 concept01-images-003.json에 보존했다. 캡션 검사에서 원본 보기 링크 문구를 제외하도록 검사 범위를 정정했으며, 제품 내용 결함은 없었다.

01 첫 화면 표시 91.732초는 마지막 관찰 간격 약 3초를 포함하고, 재제출 화면 표시 69.336초는 관찰 간격을 포함한 상한이다. 모델/DB 시간과 이 구분이 매뉴얼에 반영되어 있다. 01 이미지 대응 검수는 통과했으며 문서 Browser 렌더, 모바일·오프라인 검수는 미실행이다. 03/04/05의 실제 이미지 검수도 아직 대기한다.

## MR-004 헤더 키보드 초점 대비

01/03/04/05 공통 매뉴얼은 a:focus-visible의 #176ac6 테두리를 #173c48 헤더 위에도 사용한다. WCAG 상대 휘도 계산 결과 2.203:1로, 흰 본문 위의 5.366:1과 다르다. 작성자에게 헤더에만 밝은 초점색을 지정하도록 요청했다. #f4d486을 사용할 경우 헤더 대비 8.224:1이다. 이는 독립 소스 계산 결과이며 실제 키보드 표시 검수는 별도로 남는다.

MR-004 수정 후 네 문서의 header a:focus-visible만 #f4d486으로 덮는 규칙을 직접 확인했다. 본문 초점색은 유지했다. MEDIA-CLARIFY-2의 현재 관리 범위/교체·비활성 이력 사진과 점주·운영자 구분도 4개에 반영되어 계약에 부합한다. 소스 재검수 PASS, 최신 hash는 focus-media-004.json에 보존했다. 실제 문서 키보드/렌더 인수는 별도로 남는다.

## 시안 03 실제 이미지 반영 재검수

정상 캡처 6장의 복사 바이트/SHA-256·alt·캡션이 모두 일치하고 로컬 링크35개가 유효하다(concept03-images-005.json). 모든 원본을 직접 시각 검수했으며, 캡션은 실제 처리 중과 Mock unknown·시드 기술 실패를 구분한다. 수정 전 업로드 누락/사진 없는 대시보드는 포함하지 않았다. 실제 AI 완료·child/운영 등 후속 이미지 및 Browser 문서 렌더는 아직 대기한다.

시안03 최신8장 구성(첫/child 실제 답변·전후 사진·Reference 포함)을 다시 확인했다. 정상 원본을 직접 열람했고 복사/manifest 해시·alt·캡션은 verify-manual-assets.py 01 03 재실행에서01 8장/03 8장 PASS다. 현재 comparison 이미지는 날짜/버전 보완 전 배치이므로 최종 매뉴얼에서는 수정 후 캡처로 갱신한다. Browser 문서 렌더는 계속 미실행이다.

## MAN-IMG03 최신10장 재검수

작성자가 기존 비교 이미지를 날짜 수정본으로 교체하고 기준 버전 표·운영 재처리 성공을 추가한 최종10개 manifest를 인계했다. 원본3개는 독립 디자인 검수에서 직접 열람한 정상 viewport 이미지다. 본사 역할의 전후 KST 날짜, qa03_labels v1→v2 비교불가/같은 버전 개선, 시도1 expired 보존·시도2 성공/결과 적용과 영업 본문 없음이 alt/caption에 정확히 대응한다. 실제1440×900으로 재설정 후 촬영된 성공 캡처를 사용하여 앞선1280 운영3장의 오기를 재사용하지 않았다.

독립 verify-manual-assets.py 03은10개 원본 byte/hash·alt/caption·로컬 링크/anchor PASS다([최신 결과](concept03-images-latest.json)). 수정 전 날짜 없는 비교는 본문에서 제외됐다. 문서 렌더/키보드/오프라인 인수는 계속 NOT_RUN이며, 이10장의 정적·내용 PASS로 전체 시안이나 매뉴얼 완료를 선언하지 않는다.

## MAN-IMG04 정상10장 독립 검수

04의10개 원본 이미지는 모두 독립 디자인 검수에서 직접 열람한 정상 파일이다. 최신360 사진 제어행·지역 관리자 질문/근거 수정본을 사용하고 수정 전 돌출/겹침 및 비정상 축소 캡처는 제외됐다. OFC 조건 조회·점주 전후 비교·지역 관리자 상세의 역할이 각각 정확하며, 사진전체/하단단계버튼/둘째시도반영처럼 화면 밖의 내용을 보인다고 과장하지 않는다. QA04 실제0→100%,qa04_labels v1→v2 비교불가와 모델/DB/브라우저 관찰창(0.106초/8.142초)을 root 정본과 대조했다.

독립 verify-manual-assets.py 04는10개 exact byte/hash·alt/caption·로컬 링크/anchor PASS([최신 결과](concept04-images-latest.json)). HTML SHA-256 436dbbe7…은 작성자 동결본과 일치한다. 첫/child의30초 목표 미달,전체 인수 진행 중,문서 Browser/오프라인 검수 NOT_RUN이 명시된다. 독립 내용/정적 검토 PASS이며 최종 문서 렌더 완료를 뜻하지 않는다.

## MAN-IMG05 초기7장 독립 검수

이력시작·사진미리보기/형식오류 초안보존·60초 지연·본사 행렬/선택된CATEGORY v1·Reference등록전 미리보기7장을 앞선 실제 원본 검수와 대조했다. 홈의 과거 기록을 신규QA05로 오인하지 않고,4단계 기준 전체/등록성공/화면밖 결과를 보인다고 쓰지 않는다. 흰 first-result 캡처는 포함되지 않았다. 모델60.273초/DB61.422초와 브라우저51.315→63.181초/관찰창11.866초·30초미달 표현은 실제 기록과 일치한다.

verify-manual-assets.py 05에서7개 exact byte/hash·alt/caption·로컬링크/anchor PASS([최신 결과](concept05-images-latest.json)). 전체기능/디자인·문서렌더 NOT_RUN 및 후속결과/child/운영 추가대기를 명시한다. 초기7장 내용/정적 검수 PASS이며 최종 매뉴얼 PASS는 아니다.

## MAN-IMG05 최신10장 재검수

정상 첫 결과 fixed·child·양쪽 날짜·전체 버전표·모바일 마지막 열·OFC 해결·점주 해결을 앞선 직접 시각 검수와 대조했다. 기존 홈/미리보기/HQ행렬을 더한10개의 복사 바이트/해시·alt/caption·로컬링크는 verify-manual-assets.py 05 PASS다. 흰 원본 및 표가 잘린 옛 versions는 사용하지 않는다.

점주 비교라는 역할, 사진 상단만 보이는 dates 캡션, 모바일 오른쪽 열과 왼쪽 복귀 안내, OFC 수정/점주 읽기전용 구분이 정확하다. 실제60→100·동일 버전 개선2개/QA05 기준변경 비교불가, 모델/DB와 관찰창·타이머 실패의 보수적 상한을 분리한다. HTML3cc37c62…은 동결본과 일치한다. 내용/정적 PASS이며 운영 후속/Browser 문서 인수는 계속 대기한다.

## MAN-IMG05 운영 포함12장 재검수

운영 첫 화면과 첫 실패/둘째 성공 결과 반영 구간2장을 추가한12개 원본/복사·manifest/alt/caption·로컬링크 검사는 PASS다. 두 추가 원본은 이미 직접 시각 검수한 정상1440 화면이며 연결/실제작업·Mock 분리, 성공시 재처리 불가와 과거시도 보존을 정확히 설명한다. 과거 Mock fixture의 접수기간을 신규 성능으로 사용하지 않는 문구가 있다. HTMLd2f40a30…은 인계본과 일치한다. 200%/문서Browser 인수는 계속 NOT_RUN이며 이번 검토는 내용/정적 PASS다.

## MAN-RUNTIME-001 실행 안내 독립 검수

01/03/04/05의 최신 Node22.12/npm10/Python3.11/PostgreSQL14 요구와 실제 시안별dev/preview포트, stop의SIGTERM후즉시반환·DB중지전종료확인·stop전관리PID기록·별도터미널프롬프트복귀 문구를 README/docs10/CMD-01과 대조하여 PASS다. 각 동결hash는 runtime-copy.json과 일치하며 독립근거는 runtime-copy-review.json이다.

변경된4개 문서의 로컬링크/anchor·기존이미지 exact copy/alt/caption 재검사도 각각8/10/10/12장 PASS였다. 현재 프로세스나DB를 실제로 종료하지 않았다. 본 검토는 작성자의 문구보완을 독립 확인한 것이며 root 실제8088 문서렌더와 구분한다.

## D-MAN-H01 실제 헤더 메타글자 대비와 렌더 검수 인계

실제 concept-01/03/04/05-desktop1440-top.png에서 작성일·역할 안내가 어두운 헤더 위에 낮은 대비로 표시된다. 4개 HTML의 `header > p.compact`는 `.compact { color:#516977; font-size:14px }`가 `header p`의 밝은 색을 덮으며, 배경 `#173c48`과의 상대 휘도 대비는 **2.047:1**이다. 일반 글자 기준 4.5:1에 미달하므로 D-MAN-H01로 root와 product_design에 전달했다. 헤더에만 밝은 색을 적용하고 인쇄 문구를 검정으로 유지하도록 요청했다. 본문 보조글자와 레이아웃은 변경 대상이 아니다.

root 최신 배정으로 5개 매뉴얼 실제 PNG 독립 시각검수는 contract_data에게 단독 인계했다. 현재 파일은 기존 내용/정적검수와 위 결함 발견 기록이며, 전체 렌더 최종 판정을 대신하지 않는다. 02는 이 검토자가 작성한 문서이므로 root/contract_data의 독립 검수가 정본이다. 02의 7개 명명 region·tabindex0·3px 초점은 root 실제 모바일 재검수 기록에 반영되었다.

D-MAN-H01 재검수: 작성자 최종4개 HTML의 헤더 보조글자 #e1edef / #173c48 대비를 독립 계산하여 9.886:1로 확인했다. 인쇄에만 검정색을 유지하며 표 code에는 식별자 nowrap 규칙만 추가됐다. 이 세 CSS 변경을 되돌린 문자열의 hash가 각 수정 전 동결본과 일치하여 본문/레이아웃/이미지 불변을 확인했다. 원본이미지/alt/caption/로컬링크 재검사는 각각8/10/10/12장 PASS다. 소스 지적은 해소되었으며 최종 실제 화면은 contract_data 단독 렌더 검수로 인계한다. 근거 header-table-source-review.json.

## MAN-BROWSER-STATUS-001 상태 문구 독립 대조

01/03/04/05의 지정1440×900/390×844 문서·이미지·키보드·본문건너뛰기 확인 문구를 root 실행 및 contract_data independent-recheck.md와 대조했다.200% 사용자후행·OS네트워크차단오프라인 NOT_RUN,전체제품인수진행과 Markdown HTTP200/내장뷰어불가·편집기로읽기 제한을 구분하여 PASS다. 원본이미지/alt/caption·링크는8/10/10/12장 재검사 PASS,최종hash는작성자browser-status-copy.json과일치한다. 근거browser-status-copy-review.json. 실제PNG 판정은본인이반복한것이아니라배정된contract_data독립판정을참조한다.
