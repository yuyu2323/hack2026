인증 선택을 허용된 역할 헤더와 독립 쿠키에 결합하고 실제 DB account.role 검증을 추가한다. 사용자 권한 조회는 기존 로직 그대로 적용한다.
구현 완료: 기본 비활성 DEMO_MULTI_ROLE_ENABLED / DEMO_PUBLIC_ACCESS_ENABLED. 익명 공개 bootstrap도 기본 쿠키 CSRF와 Origin 필수. role 헤더 또는 media GET/HEAD 전용 query selector로 세션 분리; DB 실제 역할 재확인. 공개 write 최소 allowlist: auth login/logout/demo-sessions, submissions, operations job retry. 운영 설정/기준 변경 거부. 신규 제출/명시 재처리 전역 UTC 하루20건 UPSERT 원자제한, 트랜잭션 롤백 및 replay 제외. 기존 pending 실행은 quota 차감 범위 밖. 테스트 auth+multi+quota17개 PASS, 추가 retry 멱등 quota 포함 quota4개 PASS. 실제 PostgreSQL 병렬 검증 및 공개활성화는 미수행. 환경 flag 변경은 root 승인/배포 담당.
독립 리뷰 blocker 수정: auth_sessions.is_public_demo DB provenance 추가(migration0006). 공개모드에서 모든 new_session 발급을 mark하며 public flag OFF 시 base/role cookie 어떤 위치에 복사해도 session_for_request가401로 거부. 일반 로그인 발급도 동일 표식. 공개세션→비활성화→me/관리자 write 및 role token base 복사 회귀 포함 auth/multi/quota20개 PASS.
