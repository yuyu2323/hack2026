# DS05-001 디자인 명세
## 정보 구조
상단 브랜드·역할·사용자 → 역할별 가로 업무 탭 → 범위 탐색기(지역/매장/카테고리/UTC 기간) → 현재 탐색 조건과 작업영역. 필터 적용은 URL, 상세 링크는 return query를 보존한다. 현재 주소 복사로 필터를 저장/공유하되 대상 권한은 서버가 재검증한다.
점주: 이력 탐색 / 사진 제출 / 문의 / 알림. 영업: 매장 비교 / 매장 연결 / 진열 기준 / Reference / 제출 탐색 / 확인 이슈 / 추이 비교 / 알림. 운영자: 상태·연결 / 계정 탐색 / 기준 정보 / 처리 작업 / 운영 이력 / 공지. 운영자 영업 알림 제외(PD-003).
## 배치와 표현
1440px에서 콘텐츠 최대1600px, 범위 패널232px/본문 나머지, 외곽24px. 비교 화면은 좌우 날짜·사진·율과 기준 변화표를 나란히 배치한다. 목록은 색만이 아닌 상태 텍스트를 가진 탐색 행, 선택 컨텍스트는 상단 경로에 유지한다. 운영 계정은 좌측 검색목록과 우측 연결 상세를 사용한다.
390px에서 탐색 패널은 details로 접고 주 콘텐츠 단일열. 탭은 자체 가로 스크롤 가능, 페이지 전체는 금지. 표는 이름 있는 자체 스크롤 영역. 비교는 이전→이후 순서로 쌓는다.
## 토큰
배경 #f3f5f9, 표면 #ffffff, 본문 #182438, 보조 #4b5d73, 주색 #244fa3, 선 #7d8ca1, 선택 #e6edfc. pass #175b3b, fail #8b390b, unknown #465261. 본문16px/1.65, 보조14px, H1 30px. radius8px; 카드 그림자보다 구획선과 표제 사용. 조작44px 이상, focus3px #a24500.
## 상태·오류
null 수치 — 및 표본 이유. Mock/생성 이미지/실제 AI 분리. polling2초 종료시중단,30초지연안내. CSRF_INVALID는 초안·파일·중복키 보존하고 사용자 명시재시도, 일반403/401은 업무데이터 폐기.409는 최신버전만재조회하고입력초안유지, 사용자저장재시도. 운영자 unavailable은 사용불가, 비교 change unavailable은 비교불가.
## 입력·접근성
한국어 label, 오류요약 role alert, 하나의h1, route시main초점. 파일1..5 JPEG/PNG/10MiB 미리보기·삭제·순서. Reference 동일미리보기. 저장busy잠금·성공재조회. 사진contain과의미alt. 실비밀은소스/캡처/브라우저저장금지.
