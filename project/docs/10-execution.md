# 시안 05 실행 안내

현재 브랜치의 설치·포트·로그인·검증·종료 명령은 [README](../README.md)를 따릅니다.

## 관리 프로세스와 DB 종료

분석 완료 후 종료 전에 `.local/pids/*.json`에 기록된 PID를 확인합니다. `./stop.sh` 실행 뒤 해당 PID가 종료되었는지 확인하고 `./scripts/db.sh stop`을 실행합니다. 프로세스가 남아 있으면 DB를 유지합니다.

원래 5개 시안 통합 실행 기록은 main 브랜치에 보존되어 있습니다.
