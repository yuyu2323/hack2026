{
  "name": "storeloop",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/api-client",
    "web-concepts-04"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test:client": "node --import tsx --test packages/api-client/tests/*.test.ts"
  },
  "devDependencies": {
    "tsx": "^4.20.0",
    "typescript": "^5.9.0"
  }
}
